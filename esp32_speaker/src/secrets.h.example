#pragma once
// Шаблон локальных секретов. Скопируй в secrets.h (gitignored) и заполни.
#define WIFI_SSID "YOUR-WIFI-SSID"
#define WIFI_PASSWORD "YOUR-WIFI-PASSWORD"
#define OTA_PASSWORD "your-ota-password"
// Общий токен для TCP-каналов :4211/:4212 (должен совпадать с клиентом)
#define AUTH_TOKEN "change-me-32-hex-chars"